---
description: work-report 플러그인 설정 관리
argument-hint: [action]
allowed-tools: Read, Write, Edit, Bash, AskUserQuestion
---

work-report 플러그인의 설정을 관리한다.

## 인자 처리

- `$1`: 액션 (선택)
  - 미지정 또는 `show`: 현재 설정 표시
  - `init`: 새 설정 파일 생성 (대화형)
  - `edit`: 기존 설정 수정 (대화형)
  - `reset`: 설정을 기본값으로 초기화
  - `add-project`: 새 프로젝트 추가 (대화형)
  - `remove-project`: 프로젝트 제거 (대화형)
  - `list-projects`: 등록된 프로젝트 목록 표시

## 설정 파일 위치

프로젝트 루트: `.claude/work-report.local.md`

## 액션별 동작

### show (기본)

현재 설정을 읽어서 표시한다:

1. `.claude/work-report.local.md` 파일 확인
2. 파일이 있으면 YAML frontmatter 파싱하여 표시
3. 파일이 없으면 기본 설정값 표시

출력 형식:
```
📋 Work Report 설정

현재 설정:
┌─────────────────┬────────────────────────┐
│ 항목            │ 값                     │
├─────────────────┼────────────────────────┤
│ 언어            │ ko                     │
│ 저장 디렉토리   │ ./reports              │
│ 파일명 패턴     │ report-{type}-{date}.md│
│ 데이터 소스     │ git, claude            │
│ Git Author      │ user@example.com       │
│ Git 브랜치      │ all                    │
│ 보고서 모드     │ combined               │
└─────────────────┴────────────────────────┘

📁 등록된 프로젝트:
┌─────────────────┬────────┬────────────────────────────────────────┐
│ 이름            │ 타입   │ 위치/식별자                            │
├─────────────────┼────────┼────────────────────────────────────────┤
│ code-pilot      │ git    │ C:/workspace/code-pilot (현재)         │
│ backend-api     │ git    │ C:/workspace/backend-api               │
│ AI-작업         │ claude │ recent (최근 5개 세션)                 │
│ 스프린트-관리   │ jira   │ DEV                                    │
│ 팀-논의         │ slack  │ #dev-team                              │
└─────────────────┴────────┴────────────────────────────────────────┘

설정 파일: .claude/work-report.local.md
```

프로젝트가 없으면 "등록된 프로젝트" 섹션을 표시하지 않고 현재 디렉토리만 사용됨을 안내한다.

### init

대화형으로 새 설정 파일을 생성한다:

1. AskUserQuestion 도구를 사용하여 각 설정 항목 질문:
   - 보고서 언어 (ko/en)
   - 저장 디렉토리
   - 파일명 패턴
   - 데이터 소스 선택
   - Git author 이메일
   - Git 브랜치 범위
   - **보고서 모드 (combined/separate)** - 새로 추가
   - **추가 프로젝트 등록 여부** - 새로 추가

2. 입력받은 값으로 `.claude/work-report.local.md` 파일 생성:

```markdown
---
language: ko
output_dir: ./reports
filename_pattern: report-{type}-{date}.md
data_sources:
  - git
  - claude
git_author: user@example.com
git_branches: all

# 멀티 프로젝트 설정
report_mode: combined  # combined(통합) | separate(개별)
projects: []           # 추가 프로젝트 목록 (빈 배열이면 현재 디렉토리만 사용)
---

# Work Report 설정

이 파일은 work-report 플러그인의 설정을 저장합니다.
아래에 커스텀 템플릿을 추가할 수 있습니다.

## 커스텀 섹션 (선택사항)

보고서에 추가하고 싶은 커스텀 섹션이 있다면 여기에 작성하세요.
```

3. `.claude` 디렉토리가 없으면 먼저 생성

### add-project

새 프로젝트를 대화형으로 설정에 추가한다.

#### Step 1: 프로젝트 이름 입력
AskUserQuestion으로 프로젝트 이름을 입력받는다:
```
질문: "추가할 프로젝트의 이름을 입력해주세요"
header: "프로젝트명"
options:
  - label: "직접 입력"
    description: "프로젝트 식별용 이름 (예: my-app, 마케팅-캠페인, Q1-기획)"
```

#### Step 2: 프로젝트 타입(데이터 소스) 선택
AskUserQuestion으로 프로젝트의 데이터 소스 타입을 선택받는다:
```
질문: "프로젝트의 데이터 소스 타입을 선택해주세요"
header: "타입"
options:
  - label: "git"
    description: "Git 저장소 - 커밋, 브랜치, 코드 변경 이력 수집"
  - label: "claude"
    description: "Claude 대화 - 현재/과거 세션의 작업 내용, 논의 기록 수집"
  - label: "jira"
    description: "Jira 프로젝트 - 이슈, 스프린트, 워크로그 수집 (MCP 필요)"
  - label: "slack"
    description: "Slack 채널 - 메시지, 스레드, 활동 기록 수집 (MCP 필요)"
```

#### Step 3: 타입별 위치/식별자 입력
선택한 타입에 따라 다른 정보를 입력받는다:

**git 타입:**
```
질문: "Git 저장소 경로를 입력해주세요"
header: "경로"
options:
  - label: "직접 입력"
    description: "예: C:/workspace/my-project 또는 /home/user/projects/my-app"
```

**jira 타입:**
```
질문: "Jira 프로젝트 키를 입력해주세요"
header: "프로젝트 키"
options:
  - label: "직접 입력"
    description: "예: PROJ, DEV, MKT (Jira 프로젝트의 키값)"
```

**slack 타입:**
```
질문: "Slack 채널명을 입력해주세요"
header: "채널"
options:
  - label: "직접 입력"
    description: "예: dev-team, general, project-alpha (# 제외)"
```

**claude 타입:**
```
질문: "Claude 대화 범위를 선택해주세요"
header: "범위"
options:
  - label: "current"
    description: "현재 세션의 대화만 수집"
  - label: "recent"
    description: "최근 세션들의 대화 수집 (기간 내)"
  - label: "project"
    description: "특정 프로젝트 관련 대화만 수집 (태그/키워드 기반)"
```

**asana 타입:**
```
질문: "Asana 프로젝트 ID 또는 이름을 입력해주세요"
header: "프로젝트"
options:
  - label: "직접 입력"
    description: "예: 1234567890 (프로젝트 ID) 또는 프로젝트 이름"
```

#### Step 4: 유효성 검사
타입별로 유효성을 검증한다:

**git:**
- 디렉토리 존재 여부 확인
- .git 폴더 존재 여부 확인

**jira/slack/asana:**
- 해당 MCP 서버가 설정되어 있는지 확인
- 설정되지 않았으면 경고 메시지 표시 후 계속 진행 여부 확인

**공통:**
- 중복 등록 방지

#### Step 5: 타입별 추가 설정 (선택)

**git 타입 추가 설정:**
```
질문: "Git 설정을 커스터마이즈할까요?"
header: "Git 설정"
options:
  - label: "글로벌 설정 사용 (Recommended)"
    description: "전역 git_author, git_branches 설정 사용"
  - label: "별도 설정"
    description: "이 저장소만의 author, branches 지정"
```

**jira 타입 추가 설정:**
```
질문: "Jira 데이터 수집 범위를 설정할까요?"
header: "Jira 설정"
options:
  - label: "기본 설정 사용 (Recommended)"
    description: "담당자 기준, 서브태스크 포함"
  - label: "커스텀 설정"
    description: "JQL 필터, 포함할 이슈 타입 지정"
```

#### Step 6: 설정 파일 업데이트
수집된 정보로 `.claude/work-report.local.md`의 `projects` 배열에 추가:

```yaml
projects:
  # Git 프로젝트 예시
  - name: "backend-api"
    type: "git"
    path: "C:/workspace/backend-api"
    git_author: "dev@example.com"
    git_branches: "main"

  # Jira 프로젝트 예시
  - name: "스프린트-관리"
    type: "jira"
    project_key: "DEV"
    jql_filter: "assignee = currentUser()"

  # Slack 채널 예시
  - name: "팀-커뮤니케이션"
    type: "slack"
    channel: "dev-team"
    include_threads: true

  # Asana 프로젝트 예시
  - name: "마케팅-캠페인"
    type: "asana"
    project_id: "1234567890"
```

#### 완료 메시지
```
✅ 프로젝트가 추가되었습니다!

📁 backend-api [git]
   경로: C:/workspace/backend-api
   Git Author: dev@example.com

현재 등록된 프로젝트: 4개 (git: 2, jira: 1, slack: 1)
```

### remove-project

등록된 프로젝트를 제거한다:

1. 현재 등록된 프로젝트 목록 표시
2. AskUserQuestion으로 제거할 프로젝트 선택
3. 확인 후 `projects` 배열에서 제거

### list-projects

등록된 프로젝트 목록을 상세히 표시한다:

```
📁 등록된 프로젝트 (5개)

🔧 Git 저장소 (2개)
1. code-pilot (현재 디렉토리)
   경로: C:/workspace/code-pilot
   Author: ajsirh12@withnetworks.com (글로벌)
   Branches: all (글로벌)
   상태: ✓ 유효

2. backend-api
   경로: C:/workspace/backend-api
   Author: backend@team.com (오버라이드)
   Branches: main (오버라이드)
   상태: ✓ 유효

🤖 Claude 대화 (1개)
3. AI-작업
   범위: recent (최근 세션)
   세션 제한: 5개
   상태: ✓ 활성

📋 Jira 프로젝트 (1개)
4. 스프린트-관리
   프로젝트 키: DEV
   JQL: assignee = currentUser()
   상태: ✓ MCP 연결됨

💬 Slack 채널 (1개)
5. 팀-논의
   채널: #dev-team
   스레드 포함: true
   상태: ✓ MCP 연결됨
```

### edit

기존 설정을 대화형으로 수정한다:

1. 현재 설정 파일 읽기
2. 수정할 항목 선택 (AskUserQuestion 사용)
3. 선택한 항목의 새 값 입력받기
4. 설정 파일 업데이트

### reset

설정을 기본값으로 초기화한다:

1. 사용자에게 확인 요청
2. 확인 시 기본 설정으로 파일 재생성

기본 설정값:
```yaml
language: ko
output_dir: ./reports
filename_pattern: report-{type}-{date}.md
data_sources:
  - git
  - claude
git_author: (git config user.email 값)
git_branches: all
report_mode: combined
projects: []
```

## 설정 항목 상세

### language
- 설명: 보고서 작성 언어
- 타입: string
- 값: `ko` (한국어), `en` (영어)
- 기본값: `ko`

### output_dir
- 설명: 보고서 저장 디렉토리
- 타입: string (경로)
- 예시: `./reports`, `~/Documents/reports`
- 기본값: `./reports`

### filename_pattern
- 설명: 보고서 파일명 패턴
- 타입: string
- 변수: `{type}` (daily/weekly/monthly), `{date}` (YYYY-MM-DD)
- 기본값: `report-{type}-{date}.md`

### data_sources
- 설명: 데이터 수집 소스
- 타입: array
- 값: `git`, `claude`, `jira`, `slack`
- 기본값: `[git, claude]`
- 참고: jira, slack은 별도 MCP 서버 설정 필요

### git_author
- 설명: Git 커밋 필터링할 author 이메일
- 타입: string
- 기본값: `git config user.email` 값

### git_branches
- 설명: Git 데이터 수집 브랜치 범위
- 타입: string
- 값: `all` (모든 브랜치), `current` (현재 브랜치만), 특정 브랜치명
- 기본값: `all`

### report_mode
- 설명: 멀티 프로젝트 보고서 생성 모드
- 타입: string
- 값:
  - `combined`: 모든 프로젝트를 하나의 통합 보고서로 생성
  - `separate`: 각 프로젝트별 개별 보고서 생성
- 기본값: `combined`

### enable_session_logging
- 설명: Claude 세션 작업 내용 자동 저장 (Opt-in)
- 타입: boolean
- 값: `true` (활성화), `false` (비활성화)
- 기본값: `false`
- 동작:
  - 활성화 시 Claude 작업 완료 시점에 작업 요약이 자동 저장됨
  - 저장 위치: `{output_dir}/sessions/session-{date}-{time}.md`
  - **프라이버시 보호**: API 키, 비밀번호, 토큰 등 민감 정보는 저장되지 않음
  - 요약만 저장 (전체 대화 기록 X)
- 저장 내용:
  - 작업 유형 (버그 수정, 기능 추가, 리팩토링 등)
  - 변경된 파일 목록
  - 주요 결정사항
  - 수행 명령어 요약

### session_log_dir
- 설명: 세션 로그 저장 디렉토리 (enable_session_logging이 true일 때만 사용)
- 타입: string (경로)
- 기본값: `.claude/sessions`
- 참고: Claude의 다른 설정/로그와 같은 위치에 저장하여 일관성 유지

### projects
- 설명: 추가로 데이터를 수집할 프로젝트 목록
- 타입: array
- 공통 필드:
  - `name`: 프로젝트 식별 이름 (필수)
  - `type`: 데이터 소스 타입 (필수) - `git`, `claude`, `jira`, `slack`, `asana`
- 타입별 필드:

#### git 타입
- `path`: Git 저장소 절대 경로 (필수)
- `git_author`: Git author 오버라이드 (선택)
- `git_branches`: Git 브랜치 범위 (선택)

#### claude 타입
- `scope`: 대화 수집 범위 (필수) - `current`, `recent`, `project`
- `keywords`: 필터링 키워드 (선택, scope가 project일 때 사용)
- `session_limit`: 수집할 최대 세션 수 (선택, 기본: 10)

#### jira 타입
- `project_key`: Jira 프로젝트 키 (필수, 예: DEV, PROJ)
- `jql_filter`: 커스텀 JQL 필터 (선택)
- `include_subtasks`: 서브태스크 포함 여부 (선택, 기본: true)

#### slack 타입
- `channel`: Slack 채널명 (필수, # 제외)
- `include_threads`: 스레드 포함 여부 (선택, 기본: true)

#### asana 타입
- `project_id`: Asana 프로젝트 ID (필수)
- `include_subtasks`: 서브태스크 포함 여부 (선택, 기본: true)

- 기본값: `[]` (빈 배열 - 현재 디렉토리의 Git만 사용)
- 예시:
```yaml
projects:
  # 개발 프로젝트 (Git)
  - name: "frontend"
    type: "git"
    path: "C:/workspace/frontend-app"

  - name: "backend"
    type: "git"
    path: "C:/workspace/backend-api"
    git_author: "backend@team.com"
    git_branches: "main,develop"

  # Claude 대화 기록
  - name: "AI-작업"
    type: "claude"
    scope: "recent"
    session_limit: 5

  - name: "플러그인-개발"
    type: "claude"
    scope: "project"
    keywords: ["plugin", "work-report", "플러그인"]

  # 프로젝트 관리 (Jira)
  - name: "스프린트-백로그"
    type: "jira"
    project_key: "DEV"
    jql_filter: "assignee = currentUser() AND sprint in openSprints()"

  # 팀 커뮤니케이션 (Slack)
  - name: "팀-논의"
    type: "slack"
    channel: "dev-team"
    include_threads: true

  # 기획/마케팅 업무 (Asana)
  - name: "Q1-마케팅"
    type: "asana"
    project_id: "1234567890"
```

## 멀티 프로젝트 동작 방식

### combined 모드
- 모든 프로젝트의 커밋을 하나의 보고서에 통합
- 프로젝트별 섹션으로 구분하여 표시
- 전체 통계 요약 제공
- 파일명: `report-{type}-{date}.md`

### separate 모드
- 각 프로젝트별로 개별 보고서 파일 생성
- 파일명: `report-{type}-{project}-{date}.md`
- 예: `report-daily-frontend-2024-01-15.md`

### 프로젝트 우선순위
1. `projects` 배열에 등록된 프로젝트
2. 현재 작업 디렉토리 (projects가 비어있을 경우)

## 데이터 소스별 추가 설정

### Jira (선택적)
```yaml
jira:
  project_key: PROJ
  include_subtasks: true
```
참고: Jira MCP 서버가 별도로 설정되어 있어야 합니다.

### Slack (선택적)
```yaml
slack:
  channels:
    - general
    - dev-team
  include_threads: true
```
참고: Slack MCP 서버가 별도로 설정되어 있어야 합니다.
