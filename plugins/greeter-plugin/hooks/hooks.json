{
  "description": "세션 시작 시 자동으로 인사하는 훅",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/greet.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
